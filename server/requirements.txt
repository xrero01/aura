fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
edge-tts>=6.1          # free voice output (used with AURA_PROVIDER=openrouter)
sentry-sdk>=2.0        # optional crash monitoring, only active when SENTRY_DSN is set
# optional: RTSP/IP camera support
# opencv-python-headless
